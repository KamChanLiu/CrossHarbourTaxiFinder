'use strict';

describe('Controller: HomeCtrl', function () {

  // // load the controller's module
  // beforeEach(module('taxiStandApp'));

  // var MainCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   MainCtrl = $controller('HomeCtrl', {
  //     $scope: scope
  //   });
  // }));

  it('should understand tautology', function(){
      expect(true).toBe(true);
  });

  // it('should attach a list of taxi stands to the scope', function () {
  //   expect(scope.taxiStandLocations.length).toBeGreaterThan(0);
  // });
});
